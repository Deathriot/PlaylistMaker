<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingVertical="@dimen/mini_padding">

    <ImageView
        android:id="@+id/playlist_bottom_sheet_cover"
        android:layout_width="45dp"
        android:layout_height="45dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@drawable/ic_placeholder_45" />

    <TextView
        android:id="@+id/playlist_bottom_sheet_title"
        style="@style/Regular_text_style"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/mini_padding"
        android:layout_marginTop="@dimen/mini_padding"
        app:layout_constraintStart_toEndOf="@id/playlist_bottom_sheet_cover"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Крутые песни" />

    <TextView
        android:id="@+id/playlist_bottom_sheet_track_count"
        style="@style/Mini_regular_text_style"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="@id/playlist_bottom_sheet_title"
        app:layout_constraintTop_toBottomOf="@id/playlist_bottom_sheet_title"
        tools:text="23 трека" />
</androidx.constraintlayout.widget.ConstraintLayout>